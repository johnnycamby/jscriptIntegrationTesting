
========== Testing Angular Frontend ================
install karma
# npm install karma
# npm install karma-mocha karma-chai (makes karma work with chai & mocha)
# npm install karma-cli -g (so as to use karma in the command-line)
# karma init  (intialize karma)
> mocha (choose mocha)